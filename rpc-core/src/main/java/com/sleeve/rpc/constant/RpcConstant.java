package com.sleeve.rpc.constant;


/**
 * Rpc 常量配置
 */
public interface RpcConstant {
    // 默认前缀rpc，读取以rpc为前缀的配置文件中的内容 如 rpc.name=rpc version=1.0
    String DEFAULT_CONFIG_PREFIX = "rpc";


    // 默认的服务版本
    String DEFAULT_SERVICE_VERSION = "1.0";
}
